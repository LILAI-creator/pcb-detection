# PRD - Web PCB板缺陷检测系统

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-05-06 |
| 状态 | 初稿 |
| 作者 | hw058782111 |

## 2. 背景与目标

### 背景
PCB（印制电路板）在生产过程中可能出现多种缺陷（如短路、断路、焊盘缺失、划痕等），传统人工目检效率低、一致性差。基于深度学习的自动检测方案可显著提升检测效率和准确率。

### 目标
构建一个Web端的PCB板缺陷检测系统，用户通过浏览器上传PCB板图像，后端调用YOLOv8模型进行推理，返回缺陷类别、置信度和边界框，前端实时可视化展示检测结果。

## 3. 用户角色与场景

| 角色 | 描述 |
|------|------|
| 检测操作员 | 上传PCB图像，查看检测结果，导出报告 |
| 质量管理员 | 查看检测统计，审核检测结果 |
| 系统管理员 | 管理模型版本，配置系统参数 |

### 核心场景
1. **单图检测**：操作员上传一张PCB图像，系统返回标注缺陷的图像及详情列表
2. **批量检测**：操作员上传多张图像，系统依次检测并汇总结果
3. **结果复核**：质量管理员对可疑结果进行人工复核（确认/忽略）
4. **统计报表**：按时间段查看缺陷分布、检出率等统计信息

## 4. 功能需求

### FR-01 图像上传
- 支持上传 JPG / PNG / BMP 格式图像
- 单张图像最大 20MB
- 支持拖拽上传和点击选择
- 上传前在前端进行格式和大小校验
- 支持批量上传（最多50张）

### FR-02 缺陷检测
- 后端接收图像后调用YOLOv8模型进行推理
- 返回检测结果：缺陷类别、置信度、边界框坐标
- 检测耗时应在合理范围内（单图 < 5s）
- 模型加载位置预留，由用户提供YOLOv8模型权重文件

### FR-03 结果可视化
- 在原始图像上绘制检测边界框和标签
- 不同缺陷类别使用不同颜色标注
- 支持缩放和平移查看检测细节
- 侧栏展示检测列表，点击列表项高亮对应边界框
- 显示每个缺陷的类别名称和置信度百分比

### FR-04 历史记录
- 保存每次检测的图像和结果
- 支持按日期、缺陷类型筛选历史记录
- 支持分页浏览

### FR-05 导出报告
- 支持导出单次检测结果为图片（标注后的图像）
- 支持导出检测报告为 JSON / CSV 格式

### FR-06 统计面板
- 展示近期检测总数、缺陷检出率
- 按缺陷类别统计分布（饼图/柱状图）
- 按时间维度展示趋势（折线图）

## 5. 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 单图推理 < 5s（GPU环境下 < 1s），页面首屏加载 < 2s |
| 并发 | 支持10个用户同时使用 |
| 安全 | 上传文件类型和大小校验，防止恶意文件上传 |
| 可用性 | 界面响应式，支持 1920x1080 和 1366x768 分辨率 |
| 可维护性 | 前后端分离，模型可热替换 |
| 兼容性 | Chrome 90+、Edge 90+、Firefox 88+ |

## 6. 交互/原型规范

### 页面结构
1. **检测页（首页）**：图像上传区 + 检测结果展示区（图像标注 + 缺陷列表）
2. **历史记录页**：筛选条件 + 历史列表 + 详情弹窗
3. **统计面板页**：统计卡片 + 图表区

### 关键交互
- 上传图像后自动触发检测，显示加载状态
- 检测完成后平滑过渡到结果展示
- 边界框支持鼠标悬停高亮
- 历史记录点击可跳转到检测结果详情

## 7. 数据需求

### 检测结果数据结构
| 字段 | 类型 | 描述 |
|------|------|------|
| id | string | 检测记录唯一ID |
| image_url | string | 原始图像存储路径 |
| result_image_url | string | 标注后图像存储路径 |
| timestamp | datetime | 检测时间 |
| defects | array | 缺陷列表 |
| defects[].class | string | 缺陷类别名称 |
| defects[].confidence | float | 置信度 (0-1) |
| defects[].bbox | object | 边界框 {x, y, width, height} |

### 缺陷类别（默认，可根据模型调整）
- missing_hole（缺孔）
- mouse_bite（鼠咬）
- open_circuit（断路）
- short（短路）
- spur（毛刺）
- spurious_copper（杂铜）

## 8. 依赖与约束

| 依赖 | 描述 |
|------|------|
| YOLOv8模型 | 用户自行提供模型权重文件（.pt），系统预留加载接口 |
| Python 3.9+ | 后端运行环境 |
| Conda | 包管理与环境管理，开发环境：`conda activate yolo` |
| Ultralytics | YOLOv8推理依赖库 |
| GPU（可选） | 推理加速，无GPU时使用CPU |

| 约束 | 描述 |
|------|------|
| 前端技术 | 纯 HTML + CSS + JavaScript，不使用框架 |
| 后端技术 | FastAPI |
| 文件存储 | 本地文件系统存储上传图像和结果图像 |

## 9. 里程碑

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| P0 - 项目搭建 | 前后端项目骨架、模型加载接口 | 可运行空项目 |
| P1 - 核心检测 | 图像上传 + YOLOv8推理 + 结果可视化 | 单图检测完整流程 |
| P2 - 历史与导出 | 历史记录、导出报告功能 | 历史记录页、导出功能 |
| P3 - 统计与优化 | 统计面板、批量检测、性能优化 | 统计页、批量检测 |

## 10. 附录

### 项目目录结构（建议）

`pcb-detector/`
├── `backend/`
│   ├── `app/`
│   │   ├── `main.py`          — FastAPI应用入口
│   │   ├── `api/`             — API路由
│   │   │   ├── `detect.py`    — 检测接口
│   │   │   ├── `history.py`   — 历史记录接口
│   │   │   └── `stats.py`     — 统计接口
│   │   ├── `services/`
│   │   │   ├── `detector.py`  — YOLOv8推理服务（模型加载位预留）
│   │   │   └── `storage.py`   — 文件存储服务
│   │   ├── `models/`          — 数据模型定义
│   │   └── `config.py`        — 配置文件
│   ├── `weights/`             — 模型权重文件目录（用户提供）
│   ├── `uploads/`             — 上传图像存储
│   ├── `results/`             — 检测结果图像存储
│   └── `requirements.txt`
├── `frontend/`
│   ├── `index.html`           — 检测页
│   ├── `history.html`         — 历史记录页
│   ├── `stats.html`           — 统计面板页
│   ├── `css/`
│   │   └── `style.css`
│   └── `js/`
│       ├── `upload.js`        — 上传逻辑
│       ├── `detect.js`        — 检测交互
│       ├── `canvas.js`        — Canvas标注绘制
│       └── `api.js`           — API请求封装
└── `docs/`                    — 项目文档

### API 接口设计（概要）

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/detect` | 上传图像并执行检测 |
| GET | `/api/history` | 获取检测历史列表（支持分页、筛选） |
| GET | `/api/history/{id}` | 获取单条检测记录详情 |
| GET | `/api/stats` | 获取统计数据 |
| GET | `/api/stats/trend` | 获取趋势数据 |
| GET | `/api/defect-classes` | 获取缺陷类别列表 |
